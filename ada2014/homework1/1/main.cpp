#include <iostream>
#include <cstdlib>

using namespace std;

int main(int argc, char* argv[])
{
  int number_of_cases = 0;
  scanf ("%d", &number_of_cases);
  for (int i = 0; i < number_od_cases; ++i)
  {
    int number_of_boxes;
    scanf ("%d", &number_of_boxes);
    for (int j = 0; j < number_of_boxes; ++j)
    {
      long long w, h;

    }
  }
}
